# r3turn

This Golang script is used alongside a file which contains URLs to get the return codes of each URL. I created this for my own sake of VDPs and bug finding, as it's useful to see which URLs from a tool like dnsrecon are valid and which aren't.